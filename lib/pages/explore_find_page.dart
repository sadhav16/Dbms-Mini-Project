import 'package:flutter/material.dart';

class ExploreFindPage extends StatelessWidget {
  const ExploreFindPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Find what you need'),
    );
  }
}