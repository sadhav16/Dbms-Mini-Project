import 'package:flutter/material.dart';

class ExploreFavortitesPage extends StatelessWidget {
  const ExploreFavortitesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Fresh up on highlights'),
    );
  }
}